- name: ping playbook
  hosts: localhost
  tasks:
  - name: ping the server
    ansible.builtin.ping:

  - name: ping the server
    ansible.builtin.debug:
      msg: "Hello DevOps"
    tags:
    - devops  

  - name: ping the server
    ansible.builtin.debug:
      msg: "Hello aws"
    tags:
    - aws